import { UstawieniaStronyReklamowej } from './ustawienia-strony-reklamowej';

describe('UstawieniaStronyReklamowej', () => {
  it('should create an instance', () => {
    expect(new UstawieniaStronyReklamowej()).toBeTruthy();
  });
});
